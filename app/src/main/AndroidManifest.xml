<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.recgraf">

    <application
        android:allowBackup="true"
        android:icon="@drawable/biological"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Acercade"></activity>
        <activity android:name=".Permutaciones" />
        <activity android:name=".Recursividad" />
        <activity android:name=".GameActivity" />
        <activity android:name=".ActividadDesbloqueada" />
        <activity android:name=".ValPatron" />
        <activity android:name=".patron" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>